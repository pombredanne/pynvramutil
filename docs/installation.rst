============
Installation
============

At the command line::

    $ easy_install pynvramutil

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pynvramutil
    $ pip install pynvramutil
